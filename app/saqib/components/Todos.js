//  "use client";

//  export default function TodoApp({clickHandler, deleteItem}){
// <div>
//   {todos.map((todo) => {
//     return (
//       <li className={todo.completed ? "line-through" : ""} key={todo.id} >
//         <span onClick={() => clickHandler(todo)}>{todo.task}</span>

//         {/* Delete single task */}
//         <button onClick={() => deleteItem(todo.id)}>Delete</button>
//       </li>
//     );
//   })}
// </div>;
//  }
